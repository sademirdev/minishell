#include "minishell.h"
#include <string.h>
#include <errno.h>

// void	error_print(t_error *err)
// {
// 	char	*message;

// 	// errno = 1;
// 	message = strerror(err->errnum);
// 	perror(err->message);
// }
